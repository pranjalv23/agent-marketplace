# LLM — embeddings for routing + all agent LLM calls
AZURE_AI_FOUNDRY_ENDPOINT=https://<resource>.services.ai.azure.com/openai/v1
AZURE_AI_FOUNDRY_API_KEY=

# Agent URLs (Docker Compose internal service names)
FINANCIAL_AGENT_URL=http://agent-financials:8080
RESEARCH_AGENT_URL=http://agent-research:8081
INTERVIEW_PREP_AGENT_URL=http://agent-interview-prep:9003

# Router — which provider to use for embedding-based routing
ROUTER_PROVIDER=groq

# Auth
AUTH_JWT_SECRET=

# Server
PORT=9000
